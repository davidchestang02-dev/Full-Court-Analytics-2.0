from __future__ import annotations

from typing import Any, Dict, List


def train_model(samples: List[Dict[str, Any]]) -> Dict[str, Any]:
    """Stub trainer entrypoint for future model work."""
    return {"ok": False, "reason": "training_not_implemented", "samples": len(samples or [])}
